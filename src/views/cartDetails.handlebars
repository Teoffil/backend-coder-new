<h1>Detalle del Carrito</h1>
{{#if cart.products.length}}
    <ul>
        {{#each cart.products}}
            <li>Producto: {{this.productId.title}} - Cantidad: {{this.quantity}} - Precio: ${{this.productId.price}}</li>
        {{/each}}
    </ul>
    <p>Precio Total: ${{totalPrice}}</p>
{{else}}
    <p>El carrito está vacío.</p>
{{/if}}
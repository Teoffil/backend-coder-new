<h1>Detalle del Carrito</h1>
{{#if cart.products.length}}
    <ul>
        {{#each cart.products}}
            <li>Producto: {{this.productId.title}} - Cantidad: {{this.quantity}} - Precio Unitario: ${{this.productId.price}} - Subtotal: ${{multiply this.productId.price this.quantity}}</li>
        {{/each}}
    </ul>
    <p>Precio Total: ${{totalPrice cart.products}}</p>
{{else}}
    <p>El carrito está vacío.</p>
{{/if}}